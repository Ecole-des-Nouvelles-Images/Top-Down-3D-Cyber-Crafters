using UnityEngine;

namespace Player
{
    public class PlayerSpawner : MonoBehaviour
    {
        [Header("ID du Spawn")] public int spawnId; // ID du spawn utile à l'assignation du joueur
    }
}
